default: true
MD013: false   # line length — tutorials have long lines
MD033: false   # inline HTML allowed in tutorial steps
MD034: false   # bare URLs handled by link check, not markdownlint
MD041: false   # first-line-heading — YAML frontmatter precedes headings
MD025: false   # single-title/multiple-H1 — tutorials have a frontmatter title AND a body H1 by design
